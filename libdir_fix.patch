--- a/wrapper/Makefile	2018-04-13 13:09:23.000000000 -0600
+++ b/wrapper/Makefile-1	2018-04-20 18:20:52.857332775 -0600
@@ -8,7 +8,16 @@
 OBJDIR = $(TOP)/obj
 FEATURESRCDIR = $(TOP)/../feature/src
 PTOOLSDIR = $(TOP)/../ptools
-INSTALL_PREFIX = /usr/local
+MACHINE := $(shell uname -m)
+
+ifeq ($(MACHINE), x86_64)
+libdir = lib64
+endif
+ifeq ($(MACHINE), i686)
+libdir = lib
+endif
+
+INSTALL_PREFIX = /usr
 
 OBJS = \
 	$(OBJDIR)/alloc.o \
@@ -151,24 +160,24 @@
 
 .PHONY: install
 install:
-	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/lib
+	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)
 	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/include
 	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/share
-	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/lib/pkgconfig
-	cp $(LIBVMAF) $(DESTDIR)$(INSTALL_PREFIX)/lib/$(LIBVMAF)
+	mkdir -p $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)/pkgconfig
+	cp $(LIBVMAF) $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)/$(LIBVMAF)
 	cp src/libvmaf.h $(DESTDIR)$(INSTALL_PREFIX)/include/
 	cp -r ../model $(DESTDIR)$(INSTALL_PREFIX)/share/
-	cp libvmaf.pc $(DESTDIR)$(INSTALL_PREFIX)/lib/pkgconfig/
+	cp libvmaf.pc $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)/pkgconfig/
 
 .PHONY: uninstall
 uninstall:
-	rm -f $(DESTDIR)$(INSTALL_PREFIX)/lib/$(LIBVMAF)
+	rm -f $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)/$(LIBVMAF)
 	rm -f $(DESTDIR)$(INSTALL_PREFIX)/include/libvmaf.h
 	rm -fr $(DESTDIR)$(INSTALL_PREFIX)/share/model
-	rm -f $(DESTDIR)$(INSTALL_PREFIX)/lib/pkgconfig/libvmaf.pc
+	rm -f $(DESTDIR)$(INSTALL_PREFIX)/$(libdir)/pkgconfig/libvmaf.pc
 
 testlib: $(SRCDIR)/main.cpp
-	$(CXX) -s -o $@  $(CXXFLAGS) $^ -I $(FEATURESRCDIR) -I $(FEATURESRCDIR)/common -I $(DESTDIR)$(INSTALL_PREFIX)/include -L$(DESTDIR)$(INSTALL_PREFIX)/lib -lvmaf -pthread
+	$(CXX) -s -o $@  $(CXXFLAGS) $^ -I $(FEATURESRCDIR) -I $(FEATURESRCDIR)/common -I $(DESTDIR)$(INSTALL_PREFIX)/include -L$(DESTDIR)$(INSTALL_PREFIX)/$(libdir) -lvmaf -pthread
 
 .PHONY: clean
 clean:

--- a/wrapper/libvmaf.pc	2017-08-12 16:39:20.000000000 -0600
+++ b/wrapper/libvmaf-1.pc	2017-12-19 00:30:58.702964826 -0600
@@ -1,7 +1,7 @@
-prefix=/usr/local
+prefix=/usr
 exec_prefix=${prefix}
-libdir=/usr/local/lib
-includedir=/usr/local/include
+libdir=${prefix}/@libdir@
+includedir=/usr/include
 
 Name: libvmaf
 Description: Netflix's VMAF library
